<launch>
  <arg name="base_link_frame" default="vention_base_link"/>
  <arg name="odom_frame"      default="odom"/>
  <arg name="camera_name"     default="zed_mini"/>

  <!-- ZED Mini VIO: let the wrapper fuse camera + IMU and publish
       odom -> base_link directly. No EKF, no external TF. -->
  <include file="$(find zed_wrapper)/launch/zedm.launch">
    <arg name="camera_name"  value="$(arg camera_name)"/>
    <arg name="camera_model" value="zedm"/>
    <arg name="base_frame"   value="$(arg base_link_frame)"/>
  </include>

  <!-- Publish odom->base_link from the ZED wrapper itself. -->
  <param name="/$(arg camera_name)/zed_node/pos_tracking/publish_tf"     value="true"/>
  <param name="/$(arg camera_name)/zed_node/pos_tracking/publish_map_tf" value="false"/>
  <param name="/$(arg camera_name)/zed_node/pos_tracking/odometry_frame" value="$(arg odom_frame)"/>
  <param name="/$(arg camera_name)/zed_node/pos_tracking/base_frame"     value="$(arg base_link_frame)"/>
</launch>